# AI 论文阅读工作流

## 1. 收集与入库

1. 优先保存作者或会议提供的原始 PDF。
2. 使用 `scripts/ingest_paper.py` 导入，或手工按 `templates/paper_note.md` 建笔记。
3. 补齐作者、年份、venue、链接、主题与选择理由。
4. 初始状态设为 `inbox`，只有明确重要性后再提高优先级。

## 2. 三分钟筛选

只回答五个问题：

1. 它解决什么问题？
2. 相比最强相关工作，新意是什么？
3. 核心证据是什么？
4. 与当前研究方向有什么关系？
5. 是否值得精读或复现？

完成后把状态改为 `skimmed`、`reading` 或 `archived`，并在 [[notes/reading/阅读队列]] 记录选择理由。

## 3. 精读

按以下顺序阅读：

1. 摘要、图 1、结论：建立问题与贡献地图。
2. 相关工作：确认论文真正比较的对象。
3. 方法：重写输入、输出、目标函数、关键模块与假设。
4. 实验：检查数据、基线、预算、指标、消融和统计稳定性。
5. 附录：确认实现细节、额外结果和被正文弱化的限制。

精读笔记避免逐段翻译，重点写“主张—证据—判断—疑问”。

## 4. 连接与综合

完成单篇笔记后：

- 链接基础论文、并行方法、后续工作和反证论文。
- 把可复用概念写入 `notes/topics/`。
- 对同一问题至少比较任务定义、数据、基线、计算预算和评价指标。
- 只有证据足够时才把个人判断升级为主题结论。

## 5. 复现

复现前先新建 `notes/reproductions/` 笔记并写明：

- 要验证的最小主张。
- 成功标准和停止条件。
- 论文设定与本地设定的差异。
- 环境、版本、随机种子、数据、预算和运行命令。
- 结果、异常、失败原因与下一步。

复现不是默认要求。优先复现影响核心结论、实现成本可控且公开信息足够的部分。

## 6. 每日与周期复盘

- 每天用 `templates/daily_note.md` 记录阅读进展和新问题。
- 每周清理一次 `inbox`，避免收藏代替阅读。
- 每月更新 [[notes/topics/AI研究地图]]，合并重复主题并标记证据变化。
